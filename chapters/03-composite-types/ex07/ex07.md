## Ejercicio 10: append sobre subslice

Dado:

```go
nums := []int{1, 2, 3, 4, 5}
a := nums[:2]
```

Hacer varios `append` sobre `a`.

Imprimir `a` y `nums` despues de cada operacion.

Objetivo: entender cuando `append` pisa el array subyacente y cuando crea uno nuevo.
