## Ejercicio 03: map como argumento

Crear una función que reciba un `map[string]int` y agregue una nueva clave.

Después, imprimir el map original desde `main`.

Objetivo: comprobar que la modificación realizada dentro de la función se
observa desde `main`.
