## Ejercicio 04: punteros a structs

Creá un `User`, obtené un puntero a él y modificá un campo mediante el puntero.
Compará con un `User` recibido por valor.

Usá tanto `usuario.Nombre` como `(*usuario).Nombre` y explicá por qué Go
permite la primera forma cuando `usuario` es un puntero.
